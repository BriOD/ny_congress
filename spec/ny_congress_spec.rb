require 'spec_helper'

describe NyCongress do
  it 'has a version number' do
    expect(NyCongress::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
